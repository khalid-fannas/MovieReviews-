<%- include('partials/head') %>
<body class="bg-gray-100">
    <%- include('partials/navbar') %>

    <div class="relative">
        <%- include('partials/categories.ejs')%>
    <div class="relative p-10">
        <h1 class="text-3xl font-bold text-gray-800 mb-6">Admin Dashboard</h1>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">

            <div class="bg-white p-4 px-6 flex ietms-center justify-between rounded-lg shadow-lg">
                <div class="flex gap-4 items-center ">
                    <i class="fas fa-users text-4xl text-blue-500"></i>
                    <p class="text-xl font-semibold">Users</p>
                </div>
                <p class="text-2xl text-blue-600"><%= dashboardDetails.userCount %></p>
            </div>

            <div class="bg-white p-4 px-6 flex ietms-center justify-between rounded-lg shadow-lg">
                <div class="flex gap-4 items-center ">
                    <i class="fas fa-film text-4xl text-yellow-400"></i>
                    <p class="text-xl font-semibold">Movies</p>
                </div>
                <p class="text-2xl text-green-600"><%= dashboardDetails.movieCount %></p>
            </div>

            <div class="bg-white p-4 px-6 flex ietms-center justify-between rounded-lg shadow-lg">
                <div class="flex gap-4 items-center ">
                    <i class="fas fa-comments text-4xl text-green-500"></i>
                    <p class="text-xl font-semibold">Comments</p>
                </div>
                <p class="text-2xl text-orange-600"><%= dashboardDetails.commentCount %></p>
            </div>

            <div class="bg-white p-4 px-6 flex ietms-center justify-between rounded-lg shadow-lg">
                <div class="flex gap-4 items-center ">
                    <i class="fas fa-star text-4xl text-yellow-400"></i>
                    <p class="text-xl font-semibold">Ratings</p>
                </div>
                <p class="text-2xl text-yellow-600"><%= dashboardDetails.ratingCount %></p>
            </div>
        </div>

        <div class="bg-white p-6 rounded-lg shadow-lg mt-6">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-xl font-semibold">Users</h2>
                <div class="flex gap-4 items-center">
                    <label for="emailFilter" class="block text-sm font-medium text-gray-600">Filter By Email :</label>
                    <input type="email" id="emailUserFilter" class="mt-1 block w-80 px-4 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500" placeholder="Enter email">
                </div>
            </div>
            <div class="overflow-y-auto h-full max-h-72">
                <table class="w-full table-auto border-collapse border border-gray-300">
                    <thead class="bg-gray-200 sticky top-0 z-10">
                        <tr class="text-sm text-gray-600">
                            <th class="border p-3 text-left">ID</th>
                            <th class="border p-3 text-left">Name</th>
                            <th class="border p-3 text-left">Email</th>
                            <th class="border p-3 text-left">Role</th>
                            <th class="border p-3 text-left">Actions</th>
                           
                        </tr>
                    </thead>
                    <tbody id="userTableBody" class="text-sm text-gray-700">
                        <% dashboardDetails.users.forEach(user => { %>
                            <tr class="hover:bg-gray-100 transition duration-200">
                                <td class="border p-3"><%= user.id %></td>
                                <td class="border p-3"><%= user.name %></td>
                                <td class="border p-3"><%= user.email %></td>
                                <td class="border p-3"><%= user.role %></td>
                                <td class="border p-3 text-center">
                                    <button class="make-admin text-green-500 hover:text-green-700 transition duration-150 mr-2">
                                        <i class="fas fa-user-shield text-lg"></i> Make Admin
                                    </button>
                                    <button class="delete-user text-red-500 hover:text-red-700 transition duration-150">
                                        <i class="fas fa-trash text-lg"></i> Delete
                                    </button>
                                </td>
                            </tr>
                        <% }) %>
                    
                    </tbody>
                </table>
            </div>
        </div>

        <div class="bg-white p-6 rounded-lg shadow-lg mt-6 ">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-xl font-semibold">Movies</h2>
                <div class="flex gap-6 items-center">
                    <div class="flex gap-4 items-center">
                        <label for="titleFilter" class="block text-sm font-medium text-gray-600">Filter By Title :</label>
                        <input type="text" id="moviesTitleFilter" class="mt-1 block w-80 px-4 py-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500" placeholder="Enter title">
                    </div>
                    <button id="openMovieModal" class="p-2 px-6 bg-gradient-to-t from-yellow-500 to-yellow-300 rounded text-gray-700 font-bold hover:bg-yellow-400 transition duration-150">
                        Add New Movie
                    </button>
                </div>
            </div>
            <div class="overflow-y-auto h-full max-h-72">
                <table class="w-full table-auto border-collapse border border-gray-300">
                    <thead class="bg-gray-200 sticky top-0 z-10">
                        <tr class="text-sm text-gray-600">
                            <th class="border p-3 text-left">ID</th>
                            <th class="border p-3 text-left">Title</th>
                            <th class="border p-3 text-left">Description</th>
                            <th class="border p-3 text-left">Release Year</th>
                            <th class="border p-3 text-left">Producer</th>
                            <th class="border p-3 text-left">Image URL</th>
                            <th class="border p-3 text-left">Category</th>
                            <th class="border p-3 text-left">Duration</th>
                            <th class="border p-3 text-left">Actions</th>
                        </tr>
                    </thead>
                    <tbody id="movieTableBody" class="text-sm text-gray-700">
                        <% dashboardDetails.movies.forEach(movie => { %>
                        <tr class="hover:bg-gray-100 transition duration-200" data-movie-id="<%= movie.id %>">
                            <td class="border p-3"><%= movie.id %></td>
                            <td class="border p-3"><input type="text" class="w-full px-4 py-2 border border-gray-300 rounded-lg" value="<%= movie.title %>"></td>
                            <td class="border p-3"><input type="text" class="w-full px-4 py-2 border border-gray-300 rounded-lg" value="<%= movie.description %>"></td>
                            <td class="border p-3"><input type="number" class="w-full px-4 py-2 border border-gray-300 rounded-lg" value="<%= movie.release_year %>"></td>
                            <td class="border p-3"><input type="text" class="w-full px-4 py-2 border border-gray-300 rounded-lg" value="<%= movie.producer %>"></td>
                            <td class="border p-3"><input type="url" class="w-full px-4 py-2 border border-gray-300 rounded-lg" value="<%= movie.image_url %>"></td>
                            <td class="border p-3">
                                <select class="w-full px-4 py-2 border border-gray-300 rounded-lg">
                                    <option value="action" <%= movie.category === 'action' ? 'selected' : '' %>>Action</option>
                                    <option value="drama" <%= movie.category === 'drama' ? 'selected' : '' %>>Drama</option>
                                    <option value="comedy" <%= movie.category === 'comedy' ? 'selected' : '' %>>Comedy</option>
                                    <option value="horror" <%= movie.category === 'horror' ? 'selected' : '' %>>Horror</option>
                                    <option value="romance" <%= movie.category === 'romance' ? 'selected' : '' %>>Romance</option>
                                    <option value="scifi" <%= movie.category === 'scifi' ? 'selected' : '' %>>Sci-Fi</option>
                                </select>                                
                            </td>
                            <td class="border p-3"><input type="number" class="w-20 px-4 py-2 border border-gray-300 rounded-lg" value="<%= movie.duration %>"></td>
                            <td class="flex w-fit h-full items-center gap-4 border p-5 text-center">
                                <button class="flex gap-2 text-green-500 hover:text-green-700 transition duration-150 save-movie w-full h-full flex items-center justify-center">
                                    <i class="fas fa-save text-lg pointer-events-none"></i> <span> Save</span>
                                </button>
                                <button class="flex gap-2 text-red-500 hover:text-red-700 transition duration-150 delete-movie w-full h-full flex items-center justify-center">
                                    <i class="fas fa-trash text-lg pointer-events-none"></i> Delete
                                </button>
                            </td>
                        </tr>
                        <% }) %>
                    
                    </tbody>
                </table>
            </div>
        </div>
        <div id="movieModal" class="fixed inset-0 bg-gray-500 bg-opacity-50 flex items-center justify-center hidden z-50">
            <div class="bg-white p-6 rounded-lg shadow-lg w-1/3">
                <h3 class="text-xl font-semibold text-gray-700 mb-4">Add New Movie</h3>
                <form>

                    <div class="mb-4">
                        <label for="movieTitle" class="block text-sm font-medium text-gray-600">Title</label>
                        <input type="text" id="movieTitle" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg" placeholder="Enter title">
                    </div>
        
                    <div class="mb-4">
                        <label for="movieDescription" class="block text-sm font-medium text-gray-600">Description</label>
                        <textarea id="movieDescription" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg" rows="4" placeholder="Enter description"></textarea>
                    </div>
        
                    <div class="mb-4">
                        <label for="movieReleaseYear" class="block text-sm font-medium text-gray-600">Release Year</label>
                        <input type="number" id="movieReleaseYear" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg" placeholder="Enter release year">
                    </div>

                    <div class="mb-4">
                        <label for="movieProducer" class="block text-sm font-medium text-gray-600">Producer</label>
                        <input type="text" id="movieProducer" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg" placeholder="Enter producer name">
                    </div>
        
                    <div class="mb-4">
                        <label for="movieImageUrl" class="block text-sm font-medium text-gray-600">Image URL</label>
                        <input type="url" id="movieImageUrl" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg" placeholder="Enter image URL">
                    </div>
        
                    <div class="mb-4">
                        <label for="movieCategory" class="block text-sm font-medium text-gray-600">Category</label>
                        <select id="movieCategory" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg">
                            <option value="action">Action</option>
                            <option value="drama">Drama</option>
                            <option value="comedy">Comedy</option>
                            <option value="horror">Horror</option>
                            <option value="romance">Romance</option>
                            <option value="scifi">Sci-Fi</option>
                        </select>
                    </div>
        
                    <div class="mb-4">
                        <label for="movieDuration" class="block text-sm font-medium text-gray-600">Duration (minutes)</label>
                        <input type="number" id="movieDuration" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg" placeholder="Enter duration in minutes">
                    </div>
        
                    <div class="flex gap-4 justify-between w-full">
                        <button type="button" id="closeMovieModal" class="w-full p-2 px-6 bg-red-500 rounded border text-white font-bold hover:bg-red-600">Cancel</button>
                        <button type="submit" class="w-full p-2 px-6 bg-blue-500 rounded border text-white font-bold hover:bg-blue-600">Save</button>
                    </div>
                </form>
            </div>
            
        </div>
    </div>
  
    </div>
    <script src="/js/dashboard.js"></script>
</body>
</html>